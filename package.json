{
  "name": "cenittab",
  "version": "1.2.0",
  "description": "CenitTab - A Chrome extension new-tab page",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@vitest/ui": "^1.6.0",
    "vitest": "^1.6.0"
  }
}
